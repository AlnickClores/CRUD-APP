"use client"; // render on client side
import React, { useState } from "react";

const ButtonEdit = () => {
  return <button className="btn btn-warning p-3 mr-3">Edit</button>;
};

export default ButtonEdit;
